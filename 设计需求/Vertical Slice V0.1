对，这一步就开始把前面的设计第一次“跑起来”。

我建议 **Vertical Slice V0.1 不碰正式世界观、不用正式角色名，也不追求恐怖文学质量**。它本质上是一套机制试验场，但玩家从头到尾应该能玩出一个完整的小故事。

# Vertical Slice V0.1

先给它一个内部测试名：

> **VS-001：23:30 的敲门声**

名字只是测试代号，以后可以整个删掉。

它只验证一件事情：

> **第一轮玩家来不及理解一个事件 → 循环后利用已知信息提前布局 → 改变事件 → 获得第一轮不可能获得的新信息。**

---

# 1. 地图只需要三个 Location

```text id="cd3r69"
            【走廊】
             /   \
            /     \
      【值班室】 【仓库】
```

### 值班室

循环开始地点。

存在：

```text id="qlz8yf"
无线电
值班记录
NPC-A
```

### 走廊

交通节点。

存在：

```text id="okf5nt"
挂钟
仓库门
值班室门
```

### 仓库

第一轮最关键的地点。

存在：

```text id="ldqmh1"
锁门
货架
后门
未知事件
```

足够了。

---

# 2. 单轮只有30分钟

```text id="if7fm5"
23:10
↓
23:40
LOOP
```

这会让我们非常容易测试时间系统。

移动成本：

```text id="4g6ccp"
值班室 → 走廊    2分钟
走廊 → 仓库      3分钟
```

基础操作：

```text id="0s6p5k"
观察        0分钟
简单交谈    2分钟
调查        3～5分钟
移动        2～3分钟
等待        自选
```

暂时不用追求真实。

我们是在测试决策压力。

---

# 3. NPC只有一个

叫：

```text id="gchqxd"
NPC-A
```

初始：

```text id="ts0m9v"
23:10
值班室
```

他知道：

```text id="zujdgg"
仓库今晚可能有异常
```

但不会主动告诉玩家。

第一次玩家问：

> 【仓库有什么？】

NPC-A：

> “一些旧东西。别乱碰。”

获得：

```text id="rmwm75"
Testimony_01

NPC-A声称仓库里只有旧物。
```

注意不是：

> 仓库里只有旧物。

---

# 4. 核心事件：23:30敲门

后台 Event：

```yaml id="j6bc01"
event:
  id: event_knock

  trigger:
    type: TIME

  conditions:
    - time >= 23:30
    - event_knock_triggered == false

  repeat:
    mode: ONCE_LOOP

  priority: 80
```

但是这个事件有一个有趣规则：

> **声音来自仓库里面。**

而仓库门：

> 从外面锁着。

于是23:30：

如果玩家在走廊：

> 三声很轻的敲击从仓库门后传来。

```text id="4k8q9p"
咚。

……

咚。

……

咚。
```

如果玩家在值班室：

> NPC-A突然停下说话，朝门外看了一眼。

如果玩家在仓库——

**这是第二轮才有可能轻易做到的事情。**

表现完全不同。

---

# 5. 第一轮玩家不知道23:30会发生什么

开局：

```text id="uxpk9p"
23:10
值班室
```

玩家可能：

```text id="r8qu6u"
调查无线电      +5

问NPC-A         +2

调查值班记录    +5

移动到走廊      +2
```

时间已经：

```text id="0hh3j7"
23:24
```

他可能再看看挂钟、仓库门。

然后：

```text id="0ag0xe"
23:30
```

**敲门 Event。**

---

# 6. 第一轮的三个 Clue

控制得非常少。

### Clue A

值班记录：

> **仓库在22:00完成检查并从外部上锁。**

```text id="0tzhyw"
supports:
warehouse_locked_from_outside
```

---

### Clue B

23:30：

> 仓库内部出现三次敲门声。

```text id="rbocn4"
supports:
something_inside_warehouse_at_2330
```

---

### Clue C

询问NPC-A以后：

> “里面不可能有人。”

记录为：

```text id="fdjiyl"
NPC-A声称23:30时仓库内不可能有人。
```

于是玩家得到第一个非常简单的矛盾：

```text id="07bxqi"
仓库从外部锁住
+
内部有人/某物敲门
+
NPC说里面不可能有人
```

这已经足够制造第一个问题：

> **23:30仓库里面到底是什么？**

---

# 7. 第一轮不要让玩家轻易进去

比如仓库门：

```text id="tvcdsr"
locked = true
```

NPC-A持有钥匙。

玩家第一轮可以：

> 【要求NPC-A开门】

但是他拒绝。

如果玩家在23:30以后强烈要求：

NPC-A可能终于答应。

例如：

```text id="p1dx0x"
23:34 开门
```

进入仓库：

空的。

后门也锁着。

什么都没有。

这样第一轮会留下一个很好的感觉：

> **我来晚了。**

---

# 8. 第一轮结束

23:40。

比如出现一个暂时不用解释的 Transition Event：

```text id="bthlzs"
灯灭。
无线电里传来一句无法辨认的话。
```

然后：

```text id="qsm3kq"
LOOP
```

玩家重新回到：

```text id="npp0a2"
23:10
值班室
```

NPC-A：

> “怎么了？”

跟第一次一样。

---

# 9. 但是玩家不一样了

Meta State：

```text id="e9vb5g"
Memory:

knows_knock_time = true
knows_warehouse_locked = true
```

Notebook：

```text id="o5o0ku"
【上一轮】

23:30
仓库内部出现三次敲门声。
```

于是第二轮第一次出现我们的核心机制：

```text id="v2f4o8"
【记忆】23:30仓库会出现敲门声
```

---

# 10. 第二轮出现真正的新玩法

玩家现在可以选择：

> **我提前进去。**

但是钥匙还在NPC-A身上。

于是我们需要第一条：

## Knowledge Shortcut

因为上一轮玩家知道NPC-A最后会答应开门。

第二轮在23:10就可以出现：

> 【记忆】告诉NPC-A：23:30仓库里会传出敲门声。

NPC-A：

> “你怎么知道？”

这时候他当然不一定相信。

但如果玩家上一轮获得：

```text id="aobwqv"
Clue A + B + C
```

就可以进一步：

> 【坚持让他现在打开仓库】

最终：

```text id="f3vfo6"
23:15
仓库打开
```

第一轮：

```text id="u7fw2p"
23:34
```

第二轮：

```text id="ifnsmy"
23:15
```

**玩家用知识节省了19分钟。**

这就是我们必须验证出来的爽感。

---

# 11. 玩家提前进入仓库

现在第一次获得：

```text id="cpm6fy"
Scene:
warehouse_pre_event
```

第一轮永远看不到这个Scene State。

里面：

```text id="bq7ht3"
货架
后门
一张桌子
墙壁
```

玩家可以调查。

但时间有限。

例如现在：

```text id="vt5bdy"
23:17
```

距离23:30还有13分钟。

调查：

```text id="04t6j5"
货架      5分钟
后门      5分钟
桌子      5分钟
墙壁      5分钟
```

**只能选两个。**

这非常关键。

即使玩家已经拥有上一轮知识，

> 他依然不能把所有东西点完。

---

# 12. 23:30真正的反转

如果玩家在仓库里面等待：

```text id="exmsdb"
23:29

……

23:30
```

然后：

> **敲门声从仓库外面响起。**

第一轮：

> 玩家在外面，声音像从里面传来。

第二轮：

> 玩家在里面，声音却从外面传来。

这时候出现：

```text id="sdf3tt"
Loop Discrepancy_01
```

这非常适合我们的游戏。

玩家会开始怀疑：

> 那声音到底从哪来？

---

# 13. 但我们再给一个物理证据

玩家打开门。

走廊没人。

但是：

> 门外地面上出现一个东西。

暂时叫：

```text id="jj02ka"
Object-X
```

比如一个不涉及正式剧情的测试物件。

玩家获得：

```text id="zfbf4b"
Clue D:
23:30敲门以后，仓库门外出现Object-X。
```

第一轮这个东西：

**不存在。**

于是完成：

```text id="pjq5ip"
上一轮 Knowledge
↓
提前行动
↓
改变观察位置
↓
获得全新 Clue
```

核心闭环成功。

---

# 14. 第三轮测试 Anomaly

第三轮不要继续加很多剧情。

只做一件事。

玩家已经确认：

```text id="n8l4gp"
23:30
敲门
```

第三轮：

玩家23:25就站在仓库等。

23:30。

……

什么都没有。

23:31。

……

还是没有。

23:32。

**三声敲击从值班室方向传来。**

这就是：

```text id="8pr6wl"
Anomaly Stage:
Stable
→ Subtle
→ Distorted
```

玩家第一次意识到：

> **我学到的规律不是永远可靠的。**

---

# 15. Prototype Ending

第三轮之后不用解释真相。

只需要给一个测试结束：

例如玩家回到值班室。

NPC-A看着玩家：

> “你刚才是不是一直在等那个声音？”

玩家从来没有告诉过他。

然后：

```text id="b8yzkm"
Vertical Slice Complete
```

结束。

这已经足够。

因为我们的目的不是讲故事。

而是验证：

> 玩家会不会立刻想：

> **“他为什么知道？”**

如果会，我们成功了。

---

# 16. 这个V0.1到底测试了多少系统？

实际上几乎把所有核心都碰了一遍：

```text id="q78mrc"
Location             ✓
Scene Resolver        ✓
Block Narrative       ✓

Observe               ✓
Investigate           ✓
Talk                  ✓
Move                  ✓
Wait                  ✓
Memory Action         ✓

Game Time             ✓
Event Queue            ✓
Timed Event            ✓
Hidden Event           ✓

NPC State              ✓
Clue                   ✓
Testimony              ✓
Claim                  ✓
Timeline               ✓
Knowledge              ✓

Story Node             ✓
Narrative Requirement  ✓

Loop                   ✓
World Reset            ✓
Memory                 ✓

Discrepancy            ✓
Anomaly                ✓
```

Echo甚至可以暂时不加。

这样我们第一次开发不会一下承担所有复杂度。

---

# 17. V0.1真正的数据规模非常小

我建议直接锁：

| 内容                    |   V0.1 |
| --------------------- | -----: |
| Location              |      3 |
| NPC                   |      1 |
| Loop                  |      3 |
| Story Main Node       |    2～3 |
| Action                | 约20～30 |
| Core Event            |    3～5 |
| Hidden/Optional Event |    3～5 |
| Clue                  |    5～8 |
| Claim                 |    3～4 |
| Knowledge             |    2～3 |
| Memory Action         |    2～4 |
| Anomaly变化             | 1次主要变化 |
| Ending                | 1个测试结局 |

这已经足够写代码。

---

# 18. V0.1验收重点甚至不是“剧情好不好”

找一个完全不知道设计的人试玩。

第一轮结束以后什么都别告诉他。

看第二轮。

如果他自己说：

> **“那我这次提前去仓库等着。”**

这是非常强的成功信号。

因为这证明我们没有靠教程告诉他：

> “循环以后请利用上一轮的信息。”

而是玩法自己教会了他。

第三轮如果他开始说：

> “不对啊，上次明明是23:30。”

那第二层也成立了：

> **玩家已经开始主动建立并修正世界模型。**

---

## Vertical Slice V0.1 的核心验收

最后我们只看三个瞬间：

**第一次：**

> “仓库里面为什么有人敲门？”

**第二次：**

> “我这次提前进去看看。”

**第三次：**

> “不对，上次不是这样的。”

如果一个玩家自然地产生这三个念头，我们前面设计的 **Loop + Knowledge + Time + Event + Anomaly** 就基本被证明是可玩的。

而现在我建议**先不要继续设计 V0.2**。

下一步已经可以正式进入**技术实现设计**：确定这个 V0.1 用什么技术栈、项目目录怎么分、State 数据怎么定义、Location/Event/Action 是 JSON/YAML 还是代码、存档格式是什么，以及第一个可以点击运行的版本应该怎么拆开发顺序。
