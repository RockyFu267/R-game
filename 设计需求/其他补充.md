有，而且考虑到这是你第一次真正做互动文字游戏，我建议**玩法一定要克制**。我们现在最容易犯的错误，就是因为什么都能做，最后做成一个系统很多但不好玩的半成品。

我会把玩家操作压缩成 **5 个基础动作 + 1 个循环专属动作**。

### 一、玩家进入场景后，只做这五件事

**① 观察**

获取表面信息，不消耗或者只消耗极少时间。

例如进入一个房间：

```text
宿舍

房间里有四张床。
窗户没有关严。
桌上放着半杯水。

【观察窗户】
【观察桌子】
【观察床铺】
```

观察主要负责回答：

> **“这里有什么不对劲？”**

它是发现异常的第一层。

---

**② 调查**

这是比观察更深入的行为，而且应该有成本。

```text
【翻找床铺】
【检查桌子下面】
【拆开收音机】
```

可能：

```text
时间 +10分钟
获得线索
触发事件
被NPC发现
改变世界状态
```

所以：

> 观察 = 看见
> 调查 = 动手

这两个一定要分开。

---

**③ 交谈**

和当前场景里的 NPC 互动。

但第一版千万别做复杂对话系统。

例如：

```text
老陈坐在床边抽烟。

【问昨晚发生了什么】
【问失踪的人】
【质问他的说法】
【什么也不说】
```

这里以后可以根据：

```text
线索
关系
Memory
当前循环
```

改变可用选项。

这会是我们信息恐怖非常重要的来源。

---

**④ 行动 / 使用**

我建议把“拿东西、开门、使用道具、破坏东西”等统一成 **Action**。

例如：

```text
【打开柜子】
【使用钥匙】
【拿走照片】
【关掉无线电】
【锁门】
```

第一版不要设计“背包里选物品→点击对象→组合”这种复杂系统。

如果玩家有正确物品，直接出现：

> 【使用钥匙打开铁门】

就够了。

---

**⑤ 移动 / 等待**

控制空间和时间。

```text
【前往走廊】
【回宿舍】
【去找队长】

【等十分钟】
【守到午夜】
【睡觉】
```

这个其实非常重要。

因为我们的游戏有循环以后：

> **时间本身就是一种资源。**

玩家第一次不知道：

```text
23:40 会发生事件A
```

第二轮知道以后，就可以：

> 【23:30提前去仓库】

于是改变事情。

这就把“循环”真正变成玩法了。

---

# 第六个最重要：【记忆】

这个我建议做成我们的特色系统。

**第一轮几乎没有。**

循环以后开始出现：

```text
【记忆】我见过这个地方。
```

或者：

```text
【记忆】十分钟以后这里会停电。
```

甚至：

```text
【记忆】他说谎了。
```

这些不是普通选项。

它们来自：

**Memory State。**

例如第一次：

```text
老陈：
“昨晚我一直在宿舍。”

① 相信他
② 继续追问
③ 离开
```

第二轮：

```text
老陈：
“昨晚我一直在宿舍。”

① 相信他
② 继续追问
③ 离开

④【记忆】昨晚23:10我在仓库见过你
```

点④以后，进入以前不存在的剧情。

这就是我们传统版本里非常重要的：

> **循环 = 解锁新的交互权限。**

而不是单纯重复剧情。

---

# 场景结构也因此非常简单

每个 Scene 可以理解成：

```text
SCENE
│
├── 场景描述
│
├── 可观察对象
│
├── 可调查对象
│
├── 当前NPC
│
├── 可执行动作
│
├── 可移动地点
│
├── 当前可触发事件
│
└── Memory Actions
```

玩家看到的则非常干净：

```text
────────────────────

【值班室】

23:17

灯管偶尔发出轻微的电流声。

老陈坐在桌边。
桌上放着一台无线电。
北面的门通往走廊。

────────────────────

观察

> 看无线电
> 看老陈

行动

> 和老陈谈谈
> 检查无线电
> 前往走廊

记忆

> 【我记得23:40会发生什么】

────────────────────
```

当然正式 UI 不一定真长这样，我们现在只讨论逻辑。

---

# 但我想给“调查”增加一个约束

**不能让玩家把所有选项全部点一遍。**

这是传统文字冒险特别容易出现的问题：

```text
调查桌子 ✓
调查床 ✓
调查柜子 ✓
问NPC问题1 ✓
问题2 ✓
问题3 ✓
问题4 ✓
```

最后玩家根本不判断，就是“菜单清空”。

我们的解决办法非常适合循环游戏：

> **大部分有意义的行动都会推进时间。**

例如：

| 行为   |       时间 |
| ---- | -------: |
| 普通观察 |  0～1 min |
| 深入调查 | 5～15 min |
| 对话   | 2～10 min |
| 移动   | 5～20 min |
| 等待   |     玩家指定 |
| 关键行动 |      视情况 |

假设现在：

**23:17**

玩家知道23:40可能发生某件事情。

只剩23分钟。

那么：

> 调查房间 10分钟
> 去仓库 8分钟
> 找老陈谈话 7分钟

**你不可能全做。**

玩家必须判断：

> 我到底相信哪条线索？

这就从“点选项看小说”变成了**决策游戏**。

---

# 但是不要让玩家觉得自己被时间追着跑

这是另一个风险。

所以我建议采用：

**行动推进时间，而不是现实时间推进。**

玩家坐在电脑前思考20分钟：

> 游戏世界一秒都不走。

只有点击：

> 【调查房间】

才：

```text
23:17 → 23:27
```

所以玩家可以慢慢思考，没有操作压力。

---

# 我还建议增加一个很轻的“笔记系统”

不是复杂任务列表。

只记录玩家已经确认或者认为重要的信息：

```text
【记录】

人物
老陈
· 声称昨晚一直在宿舍

事件
· 23:40 曾发生停电

地点
仓库
· 门在第一次循环中是锁着的

未解问题
· 无线电里的声音是谁？
```

注意这里最好区分：

**事实**和**玩家认知**。

笔记里写：

> “老陈声称昨晚在宿舍。”

而不是：

> “老陈昨晚在宿舍。”

因为后者系统替玩家判断真假了。

这非常符合我们的信息恐怖设计。

---

## 所以第一版我建议就做到这里

玩家核心操作：

**观察 / 调查 / 交谈 / 行动 / 移动与等待 /【记忆】**

辅助系统只有：

**时间 + 线索/笔记 + 人物关系 + 物品 + 循环状态。**

我甚至暂时**不要**血量、饥饿、理智值、技能树、战斗系统、复杂背包、人物属性。

因为我们真正要让玩家玩的东西应该是：

> **有限时间里获取信息 → 判断谁/什么可信 → 做选择 → 承担不可逆后果 → 循环 → 利用上一轮认知重新布局。**

这样第一款游戏的工程量可控，同时已经有一个很明确的核心玩法。

我认为这套可以直接进入《传统文字游戏需求设计》，但你刚才是让我给建议，所以我先不替你定：**如果你认可，我们就把“5+1 操作模型 + 行动推进时间 + 笔记系统”正式定下来。**



哈哈，那这套就正式定了。我已经把 **“5+1 操作模型 + 行动推进时间 + 轻量笔记系统”**写进《传统文字游戏需求设计》的已确认部分。

而且现在我觉得我们的游戏已经从“想做一个《R点》味道的文字恐怖游戏”，逐渐变成了一个**有明确玩法身份的东西**。

它不是：

> 看文字 → ABCD → 看下一段文字

而是：

> **我现在在哪里？ → 我知道什么？ → 我还剩多少时间？ → 谁的话可信？ → 这轮我要调查什么？ → 我愿意放弃什么？ → 事情发生 → 循环 → 我带着知识重新布局。**

尤其是“**不能把所有选项点一遍**”这条，我认为特别重要。它让玩家的选择第一次真正有了成本。

下一步我建议开始设计 **Scene（场景）系统**。

因为现在我们已经知道玩家能做什么了，那么就该回答：

> **玩家到底在什么东西里面做这些事情？**

我们可以把一个 Scene 设计成游戏最基本的“容器”：

```text
Scene
├── 时间
├── 地点
├── 场景描述
├── NPC
├── 可观察对象
├── 可调查对象
├── 可执行 Action
├── 可前往地点
├── 当前 Event
├── 可发现 Clue
├── Memory 选项
└── 进入/离开条件
```

然后还有一个很关键的问题值得一起解决：

**Scene 和 Story Node 到底是什么关系？**

我已经有一个比较明确的想法：**地点不应该等于剧情节点。**

同一个“值班室”，第一次去、停电以后去、某个人死后再去、第二次循环再去——应该是**同一个 Location，但呈现完全不同的 Scene State**。

如果我们把这一层设计好，以后写剧情时会轻松很多，也不会产生几百个“值班室01、值班室02、值班室03”这种维护地狱。

这应该是我们下一步非常值得定下来的架构。


好，这条也正式定下来。

现在我们的底层开始很清楚了：

```text id="4x1uep"
Location
= “这是哪里”
= 相对稳定的物理空间

Scene State
= “这个地方现在是什么样”
= 时间 + NPC + 世界状态 + 当前循环 + 已发生事件

Story Node
= “故事现在进行到了哪里”
= 控制哪些剧情内容已经开放

Event
= “此刻发生了什么”
= 对世界和场景产生变化
```

所以同一个地点完全可以这样变化：

```text id="xktc9r"
【值班室】 Location_03

第一次进入
→ 正常 / NPC A在场 / 无线电正常

23:40之后
→ 停电 / NPC A离开 / 无线电出现异常

某事件之后
→ 房门损坏 / 出现新调查点

Loop 2
→ 房间基本恢复
→ 但出现 Echo
→ 玩家拥有新的【记忆】选项

Loop 4
→ Anomaly提高
→ 本来不应该出现的东西出现
```

但是数据库里始终只有一个：

```text id="pcl19u"
Location_03
```

这对以后开发非常重要。

接下来我们就可以正式设计 **Scene Resolver（场景解析器）**：

> 玩家进入 `Location_03` 时，系统到底根据哪些状态，计算出“这一次玩家应该看到什么、谁在这里、有哪些操作、哪些线索可以调查、哪些【记忆】选项出现”。

这个东西一旦定下来，我们的 **Location + State + Event + Loop + 5+1 操作** 就真正串起来了。

而且这是第一块已经非常接近以后实际代码的数据结构。我建议下一步就啃它。

好。这里我建议稍微认真一点，因为 **Scene Resolver 是我们传统版本第一个真正意义上的“核心引擎”**。

它解决的核心问题只有一句话：

> **玩家此时此刻来到这个地点，游戏应该呈现什么？**

## 一、先确定整体关系

我建议最终结构是：

```text
                    ┌── World State
                    │
                    ├── Story State
                    │
                    ├── Time State
                    │
Location ───────────┼── NPC State
                    │
                    ├── Player State
                    │
                    ├── Memory State
                    │
                    ├── Echo State
                    │
                    └── Anomaly State
                           │
                           ▼
                    Scene Resolver
                           │
                           ▼
                     Current Scene
```

所以 **Scene 本身不是提前写死的一篇文字**。

Scene 是系统在玩家进入地点时，根据当前状态**解析出来的结果**。

---

# 二、Location 只保存“相对永久”的东西

例如：

```yaml
location:
  id: guard_room

  name: 值班室

  description_base:
    - 一间狭窄的值班室。
    - 北侧连接走廊。
    - 东侧有一扇窗。

  objects:
    - radio
    - desk
    - locker
    - window

  exits:
    north: corridor
    south: dormitory
```

这里不要写：

> 老陈坐在桌边。

因为老陈今天在，明天可能不在。

也不要写：

> 无线电正在播放奇怪声音。

因为这是 Event / State。

Location只回答：

> **这里本质上是什么地方？**

---

# 三、Scene Resolver 每次进入都重新计算

玩家：

> 【前往值班室】

系统执行：

```text
resolve_scene(
    location = guard_room,
    current_state
)
```

然后按顺序解析。

我建议固定成 **8步**。

---

## ① 基础 Location

先读取：

```text
值班室

Objects:
radio
desk
locker
window

Exits:
corridor
dormitory
```

这是骨架。

---

## ② World Modifier

检查世界发生过什么。

例如：

```text
power_off = true
window_broken = true
radio_destroyed = false
```

那么：

```text
基础：
窗户

↓

当前：
破碎的窗户
```

或者：

```text
基础：
radio = usable

↓

当前：
radio = no_power
```

---

# ③ Time Modifier

时间影响场景。

比如：

```text
06:00–18:00
day

18:00–23:00
evening

23:00–06:00
night
```

甚至某地点可以：

```text
IF time >= 23:40
    enable observation_A
```

但这里有一个设计原则：

**时间本身最好少直接触发剧情。**

更推荐：

```text
23:40
↓
Event_017触发
↓
Event改变World State
↓
Scene Resolver读取变化
```

而不是到处写：

```text
if time > 23:40
```

否则以后会非常难维护。

---

# ④ NPC Resolver

然后检查：

> 谁现在应该在这里？

例如：

```text
NPC Chen

alive = true
location = guard_room
available = true
```

于是加入：

```text
Scene NPC:
陈
```

如果：

```text
alive = false
```

正常情况下自然不出现。

但是以后 Anomaly：

```text
anomaly >= 4
AND
special_condition
```

可能又出现。

注意：

**Scene Resolver不判断这是不是鬼。**

它只知道：

> 当前规则告诉我，这里需要呈现 NPC_Chen。

这个思想很重要。

---

# 五、Object Resolver

现在计算每个东西能不能：

**看 / 调查 / 操作。**

比如 Radio：

```yaml
radio:

  observe:
    always: true

  investigate:
    condition:
      radio_investigated == false

  actions:

    listen:
      condition:
        radio_destroyed == false

    repair:
      condition:
        has_item: fuse
```

于是玩家第一次来：

```text
【观察无线电】
【检查无线电】
【打开无线电】
```

后来无线电坏了：

```text
【观察无线电】
【检查损坏的位置】
```

如果玩家拿到了保险丝：

```text
【更换保险丝】
```

这就是状态驱动选项。

---

# 六、Clue Resolver

这一层我建议**不要直接显示线索**。

而是判断：

> 当前有哪些线索具备“可发现资格”。

例如：

```text
Clue_017

location:
guard_room

source:
radio

requirements:
  radio_investigated = true
  event_03_completed = true

exclude:
  clue_017_discovered = true
```

那么 Scene Resolver只告诉系统：

```text
clue_017:
AVAILABLE
```

玩家还必须执行：

> 【检查无线电】

才能真正获得。

这样：

**“线索存在”**

和

**“玩家发现线索”**

是两件不同的事情。

这点必须保留。

---

# 七、Memory / Echo / Anomaly Resolver

这里开始体现我们的特色。

正常选项可能：

```text
【打开无线电】
```

Memory State满足以后：

```text
【记忆】不要打开无线电
```

或者：

```text
【记忆】调到某个频率
```

Echo存在：

> 桌角似乎有什么划痕。

于是新增：

```text
【观察划痕】
```

Anomaly达到条件：

> 房间里似乎多了一把椅子。

于是：

```text
【观察多出来的椅子】
```

但是玩家UI永远不会看到：

```text
[Anomaly Level 3]
```

他只会觉得：

> 等等……

> **上次这里有这东西吗？**

这正是我们想要的。

---

# 八、Story / Event Resolver

最后才检查剧情层。

例如当前：

```text
Story Node = Chapter_2_A
```

允许：

```text
Event:
E17
E18
E23
```

禁止：

```text
E30+
```

然后检查有没有：

**进入场景立即触发的事件。**

例如：

```text
Event_18

trigger:
ENTER_LOCATION

location:
guard_room

requirements:
  story >= chapter_2
  npc_chen_alive = true
  event_18_triggered = false
```

如果满足：

玩家进入值班室。

先不出现普通 Scene。

而是：

```text
ENTER
 ↓
Event_18
 ↓
处理结果
 ↓
更新状态
 ↓
重新 Resolve Scene
```

注意最后这一点。

**事件发生以后必须重新解析场景。**

因为 Event 可能造成：

```text
NPC离开
灯熄灭
门锁上
出现新物品
时间推进
人物死亡
```

---

# 最终 Scene Resolver 输出什么？

我建议它输出一个统一的 **Scene View Model**。

例如：

```json
{
  "location": {
    "id": "guard_room",
    "name": "值班室"
  },

  "time": "23:17",

  "description_blocks": [
    "base_guard_room",
    "night_description",
    "window_broken_description"
  ],

  "npcs": [
    "npc_chen"
  ],

  "observations": [
    "radio",
    "window",
    "npc_chen"
  ],

  "investigations": [
    "radio"
  ],

  "actions": [
    "talk_chen",
    "turn_on_radio"
  ],

  "memory_actions": [
    "remember_radio_frequency"
  ],

  "exits": [
    "corridor",
    "dormitory"
  ]
}
```

UI完全不需要理解复杂剧情。

它只负责：

> Scene Resolver 给我什么，我显示什么。

这样以后你换 Web、Unity、Godot，甚至做手机端，剧情引擎都不用重写。

---

# 还有一个非常重要的问题：描述文字怎么处理？

传统游戏没有AI，所以我们不能：

```text
根据当前所有状态动态生成一段文字
```

但是也不能为每种组合人工写完整 Scene。

否则：

```text
夜晚 + 停电 + 陈在
夜晚 + 停电 + 陈不在
夜晚 + 有电 + 陈在
夜晚 + 有电 + 陈不在
白天 + ...
```

马上组合爆炸。

我的建议是：

## Block-Based Narrative

也就是**模块化叙事文本**。

不要写：

```text
scene_guard_room_version_37
```

而是：

```text
BASE
一间狭窄的值班室，墙边摆着一张掉漆的木桌。

+

TIME_NIGHT
窗外已经完全黑了。

+

POWER_OFF
头顶的灯没有亮。

+

NPC_CHEN_PRESENT
陈坐在桌边，一言不发。

+

ECHO_03
桌角似乎多了几道你不记得的划痕。
```

最终组合：

> 一间狭窄的值班室，墙边摆着一张掉漆的木桌。
>
> 窗外已经完全黑了。
>
> 头顶的灯没有亮。
>
> 陈坐在桌边，一言不发。
>
> 桌角似乎多了几道你不记得的划痕。

**完全不需要AI。**

但已经有一定动态场景能力。

将来接AI以后，甚至可以：

```text
这些 Block
     ↓
AI润色
     ↓
更自然的场景描述
```

所以我们现在做的东西未来完全不会浪费。

---

# 我建议再定一个“优先级覆盖规则”

因为以后肯定会出现冲突。

例如：

```text
BASE:
无线电在桌上。

WORLD:
无线电被玩家砸坏。

EVENT:
无线电正在播放声音。
```

到底显示哪个？

所以所有 Modifier 应该有优先级：

```text
基础 Location
      ↓
World State
      ↓
Story State
      ↓
Event State
      ↓
Loop / Echo
      ↓
Anomaly
      ↓
特殊强制覆盖
```

高优先级可以：

```text
ADD
REMOVE
REPLACE
DISABLE
ENABLE
```

例如：

```text
radio:

BASE:
usable = true

World Modifier:
radio_destroyed
→ usable = false

Event_17:
→ wants radio_playing = true

Resolver发现：
radio_destroyed = true

结果：
Event无法让已经损坏的radio正常播放
```

**除非：**

```text
Anomaly Modifier:
force_radio_playing = true
```

这时候：

> 一个已经被玩家砸坏的无线电，

> **开始说话。**

你看，连恐怖效果都可以从规则系统本身长出来。

---

## 到这里，Scene系统其实已经完整了

现在我们有：

```text
Location
   ↓
Scene Resolver
   ← World
   ← Time
   ← NPC
   ← Object
   ← Clue
   ← Story
   ← Event
   ← Memory
   ← Echo
   ← Anomaly
   ↓
Current Scene
   ↓
5+1 Actions
   ↓
玩家选择
   ↓
状态改变
   ↓
重新 Resolve
```

我认为这套应该直接作为 **《传统文字游戏需求设计》Scene System V1**。

而下一步我建议不要继续设计更大的系统了。

我们应该开始设计最重要的数据单位之一：

> **Action 到底长什么样。**

因为玩家看到的所有：

**【调查桌子】【问老陈】【前往走廊】【等待十分钟】【记忆：调到某个频率】**

底层最好全部是同一种 Action 数据结构。

如果我们把 Action 统一掉，后面 Event、Choice、时间、道具、NPC、Memory 基本就全部串起来了。
